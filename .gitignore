# Binaries & build output
/bin/
/dist/
/site/
/completions/
*.exe

# Test & coverage
coverage.out
coverage.html
cover.out
*.test

# Local dev scratch (downloaded API docs, generation guide)
/.dev/

# Secrets / local env — never commit credentials
.env
.env.*
*.env
credentials.json

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Worktree checkouts created by the worktree skill
/.claude/worktrees/
